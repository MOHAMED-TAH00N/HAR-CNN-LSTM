# HAR-CNN-LSTM
Human Activity Recognition (HAR) using CNN + LSTM. Analyzes videos to predict actions like walking, running, boxing, hand clapping, and waving. Features: video preprocessing, CNN frame-level feature extraction, LSTM temporal modeling, output annotated videos, modular and scalable for larger datasets.
